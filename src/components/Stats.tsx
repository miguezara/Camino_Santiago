import { Footprints, Wallet, Stamp, Camera } from 'lucide-react';import { useApp } from '../contexts/AppContext';import stages from '../data/trip.json';
export default function Stats(){const{done,budget}=useApp();const walked=stages.stages.filter(s=>done.includes(s.from)).reduce((n,s)=>n+s.km,0);const spent=Object.values(budget).reduce((n,v)=>n+v,0);return <div className="stats"><div><Footprints/><b>{walked} km</b><small>recorridos</small></div><div><Wallet/><b>{spent} €</b><small>previstos</small></div><div><Stamp/><b>{done.length * 2}</b><small>sellos mínimos</small></div><div><Camera/><b>0</b><small>fotos locales</small></div></div>}
