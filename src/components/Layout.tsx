import{NavLink}from'react-router-dom';import{Home,Route,Map,Wallet,CheckSquare,Settings,BookOpen,CalendarDays,SlidersHorizontal}from'lucide-react';import type{ReactNode}from'react';
const nav=[['/',Home,'Inicio'],['/planificador',SlidersHorizontal,'Ajustar'],['/itinerario',CalendarDays,'Plan'],['/ruta',Route,'Camino'],['/mapa',Map,'Mapa'],['/diario',BookOpen,'Diario'],['/presupuesto',Wallet,'Coste'],['/checklist',CheckSquare,'Lista'],['/configuracion',Settings,'Ajustes']] as const;
export default function Layout({children}:{children:ReactNode}){return <><header><NavLink to="/" className="brand">✦ Mi Camino</NavLink><span>26 jul · 1 ago</span></header><main>{children}</main><nav>{nav.map(([to,I,label])=><NavLink key={to} to={to} end={to==='/' }><I size={19}/><small>{label}</small></NavLink>)}</nav></>}
