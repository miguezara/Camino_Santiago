import{MapPin,Euro,ExternalLink}from'lucide-react';type P={name:string;kind:string;detail:string;price?:string;coords:number[]};export default function PlaceCard({p}: {p:P}){const q=`https://www.google.com/maps/search/?api=1&query=${p.coords.join(',')}`;return <article className="card"><div className="tag">{p.kind}</div><h3>{p.name}</h3><p>{p.detail}</p><footer><span><Euro size={15}/>{p.price||'Consultar'}</span><a href={q} target="_blank"><MapPin size={15}/>Mapa <ExternalLink size={13}/></a></footer></article>}
